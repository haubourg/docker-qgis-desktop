# docker-qgis-desktop
a qgis desktop container


my personal recipes to install QGIS on linux, as many versions and configurations as needed.  One branch per version / usage 

to build an image, checkout the desired branch

then for 3.x_master build with :

docker build --no-cache  -t     qgis_local:3.x_master ./ 

